# IGNORE LIST FOR GIT
# Ship data + generated storage tree (server/app/config.py DATA_DIR, default
# server/data/) — raw/temp/drafts/published/audit_blobs/avatars, plus the
# get_data.sh sync staging dir. Never commit ship netCDF files.
server/data/

# Backend runtime state
server/.env
server/svidat.db
server/.venv/
__pycache__/
*.pyc

# Frontend
client/node_modules/
client/dist/

# OS
.DS_Store

# Local superpowers skill session state (brainstorm server pids/logs/content)
.superpowers/

# Superpowers design/plan docs — kept locally as design-rationale reference
# (see CLAUDE.md), not repo content.
docs/superpowers/

# Claude Code project instructions — kept locally, not repo content.
CLAUDE.md

# Local git worktrees used for isolated feature-branch work.
.worktrees/
